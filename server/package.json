{
  "name": "server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch index.js",
    "start": "node index.js",
    "test": "DB_PATH=:memory: node --test 'tests/*.test.js'"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
